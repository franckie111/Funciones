function multiplicarLista($lista) {
    $producto = 1;
    foreach ($lista as $numero) {
        $producto *= $numero;
    }
    return $producto;
}

$lista = array(8, 2, 3, -1, 7);

echo "El producto de los números en la lista es: " . multiplicarLista($lista);
